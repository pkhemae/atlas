# Node
TZ=UTC
PORT=3334
HOST=localhost
NODE_ENV=development

# App
LOG_LEVEL=info
APP_KEY=zKXHe-Ahdb7aPK1ylAJlRgTefktEaACi
APP_URL=http://${HOST}:${PORT}

# Session
SESSION_DRIVER=cookie

#--------------------------------------------------------------------
# CORS — production allowlist, comma-separated. Defaults to the Tauri
# webview origins when unset. Ignored in development (all origins allowed).
#--------------------------------------------------------------------
# CORS_ORIGINS=tauri://localhost,http://tauri.localhost

# Rate limiting
LIMITER_STORE=memory

# Mail
MAIL_MAILER=resend
MAIL_FROM_ADDRESS=onboarding@resend.dev
MAIL_FROM_NAME=Atlas
RESEND_API_KEY=replace-me
DRIVE_DISK=fs
